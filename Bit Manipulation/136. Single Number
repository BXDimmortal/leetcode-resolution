/----------------------------------异或操作------------------------/
class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int n = nums.size();
        int result = nums[0]; 
        for(int i =1;i<n;++i){
            result^= nums[i]; /-------------由于异或操作的交换性，因而可以得到数组中唯一的数字-------------------/
        }
        return result;
        
    }
};
/------------------------------------------------------------/
1.一个数与自己的异或得到零，任何一个数与零异或得到该数字本身
2.可以用异或操作交换数字
